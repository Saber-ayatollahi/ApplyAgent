# Saber's Job Search System — Python dependencies
# Python 3.9+ required

# Core — scrapers and fetchers
requests>=2.30
beautifulsoup4>=4.12

# Scheduling / dates
python-dateutil>=2.8

# LLM agent
anthropic>=0.40

# Data frames + UI
pandas>=2.0
streamlit>=1.30
# Poll-based auto-refresh for live progress pages (scraper/scorer running)
streamlit-autorefresh>=1.0

# Master Repo YAML sources
pyyaml>=6.0

# Cross-process file locking (safe_json helper)
portalocker>=2.7
